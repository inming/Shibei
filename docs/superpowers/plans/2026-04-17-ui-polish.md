# UI 细节优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 2026-04-17 spec 中的 6 项 UI 细节优化：tooltip 文案统一、URL 打开按钮对齐、元数据编辑入口、预览面板标注可点击跳转、PDF 导入右键入口、设置页样式对齐。

**Architecture:** 纯前端改动，不触碰 Rust 后端和 SQLite 层。每项独立成一个 commit。复用已存在的组件（`ResourceEditDialog`、`ContextMenu`、`openResource`、`initialHighlightId` 机制）和 i18n 基础设施。新增一个 `src/lib/importPdf.ts` 工具函数用于消除代码重复。

**Tech Stack:** React 18 + TypeScript 5 + Vite + `react-i18next` + CSS Modules + Tauri 2 `@tauri-apps/plugin-dialog`。

**Spec:** `docs/superpowers/specs/2026-04-17-ui-polish-design.md`

---

## Task 1: 统一设置按钮 tooltip 为"设置"

**对应 spec 第 1 节。**

**Files:**
- Modify: `src/locales/zh/common.json`
- Modify: `src/locales/en/common.json`
- Modify: `src/components/SyncStatus.tsx:68`

### - [ ] Step 1.1: 在 zh/common.json 新增 `settings` key

文件 `src/locales/zh/common.json`，在第 2 行 `"save"` 之后插入：

```diff
 {
   "save": "保存",
+  "settings": "设置",
   "cancel": "取消",
```

### - [ ] Step 1.2: 在 en/common.json 新增 `settings` key

读取 `src/locales/en/common.json`，找到 `"save"` 所在行，插入同一位置：

```diff
 {
   "save": "Save",
+  "settings": "Settings",
   "cancel": "Cancel",
```

（若 en/common.json 字段顺序与 zh 不同，在顶部合适位置添加即可，保持与 zh 对称）

### - [ ] Step 1.3: 修改 SyncStatus.tsx 使用新 key

文件 `src/components/SyncStatus.tsx`，第 68 行：

```diff
-<button className={styles.gear} onClick={() => onOpenSettings()} title={t('syncSettings')}>⚙</button>
+<button className={styles.gear} onClick={() => onOpenSettings()} title={t('settings', { ns: 'common' })}>⚙</button>
```

注意：`t()` 调用不需要修改组件顶部的 `useTranslation` 命名空间，通过 `{ ns: 'common' }` 参数跨命名空间查询即可。

### - [ ] Step 1.4: 类型检查

Run: `npm run build` 或 `npx tsc --noEmit`
Expected: 无 TypeScript 错误。

### - [ ] Step 1.5: Commit

```bash
git add src/locales/zh/common.json src/locales/en/common.json src/components/SyncStatus.tsx
git commit -m "fix(i18n): unify \"settings\" tooltip on sidebar gear button"
```

---

## Task 2: URL 打开按钮紧跟 URL、高度一致

**对应 spec 第 2 节。**

**Files:**
- Modify: `src/components/ResourceMeta.module.css:37-66`

### - [ ] Step 2.1: 修改 ResourceMeta.module.css 的 .urlRow / .url / .urlOpenBtn

替换第 37-66 行：

```css
.urlRow {
  /* keep as wrapper for potential future styling, no flex */
}

.url {
  color: var(--color-text-primary);
  word-break: break-all;
  user-select: text;
  -webkit-user-select: text;
  cursor: text;
}

.urlOpenBtn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  vertical-align: middle;
  width: 18px;
  height: 18px;
  margin-left: 4px;
  border: 1px solid var(--color-border);
  border-radius: 4px;
  background: transparent;
  color: var(--color-text-muted);
  cursor: pointer;
  font-size: 11px;
  padding: 0;
}

.urlOpenBtn:hover {
  color: var(--color-accent);
  border-color: var(--color-accent);
}
```

变化点：
- `.urlRow`：去掉 `display: flex`、`align-items`、`gap`
- `.url`：去掉 `flex: 1`
- `.urlOpenBtn`：`flex-shrink: 0` 删除；`width/height` 22→18；新增 `margin-left: 4px`、`vertical-align: middle`、`padding: 0`；`font-size` 12→11；删除 `margin-top: 1px`

### - [ ] Step 2.2: 类型/构建检查

Run: `npx tsc --noEmit`
Expected: 无错误（此任务纯 CSS，TS 不会受影响）。

### - [ ] Step 2.3: Commit

```bash
git add src/components/ResourceMeta.module.css
git commit -m "fix(preview): align URL open button with URL, match text height"
```

---

## Task 3: 元数据右上角新增编辑按钮

**对应 spec 第 3 节。**

**Files:**
- Modify: `src/components/ResourceMeta.tsx`
- Modify: `src/components/ResourceMeta.module.css`
- Modify: `src/locales/zh/sidebar.json`
- Modify: `src/locales/en/sidebar.json`

### - [ ] Step 3.1: 新增 i18n key `sidebar.metaEdit`

文件 `src/locales/zh/sidebar.json`，在 `"metaTags"` 那一行之后插入：

```diff
   "metaTags": "标签",
+  "metaEdit": "编辑",
```

文件 `src/locales/en/sidebar.json`，同样位置（`metaTags` 后）添加：

```diff
   "metaTags": "Tags",
+  "metaEdit": "Edit",
```

### - [ ] Step 3.2: ResourceMeta.tsx 新增 edit button + dialog state

完整替换 `src/components/ResourceMeta.tsx` 的文件内容为：

```tsx
import { useState, useEffect } from "react";
import { useTranslation } from "react-i18next";
import { listen } from "@tauri-apps/api/event";
import type { Resource, Tag, Folder } from "@/types";
import * as cmd from "@/lib/commands";
import { DataEvents } from "@/lib/events";
import { ResourceEditDialog } from "@/components/Sidebar/ResourceEditDialog";
import styles from "./ResourceMeta.module.css";

interface ResourceMetaProps {
  resource: Resource;
  onNavigateToFolder?: (folderId: string) => void;
}

export function ResourceMeta({ resource, onNavigateToFolder }: ResourceMetaProps) {
  const { t } = useTranslation('sidebar');
  const [tags, setTags] = useState<Tag[]>([]);
  const [folderPath, setFolderPath] = useState<Folder[]>([]);
  const [editing, setEditing] = useState(false);

  useEffect(() => {
    cmd.getTagsForResource(resource.id).then(setTags).catch(() => setTags([]));
  }, [resource.id]);

  useEffect(() => {
    cmd.getFolderPath(resource.folder_id).then(setFolderPath).catch(() => setFolderPath([]));
  }, [resource.folder_id]);

  useEffect(() => {
    const u1 = listen(DataEvents.TAG_CHANGED, () => {
      cmd.getTagsForResource(resource.id).then(setTags).catch(() => setTags([]));
    });
    const u2 = listen(DataEvents.SYNC_COMPLETED, () => {
      cmd.getTagsForResource(resource.id).then(setTags).catch(() => setTags([]));
      cmd.getFolderPath(resource.folder_id).then(setFolderPath).catch(() => setFolderPath([]));
    });
    const u3 = listen(DataEvents.FOLDER_CHANGED, () => {
      cmd.getFolderPath(resource.folder_id).then(setFolderPath).catch(() => setFolderPath([]));
    });
    return () => {
      u1.then((f) => f());
      u2.then((f) => f());
      u3.then((f) => f());
    };
  }, [resource.id, resource.folder_id]);

  return (
    <div className={styles.meta}>
      <button
        className={styles.editBtn}
        title={t('metaEdit')}
        onClick={() => setEditing(true)}
      >
        ✎
      </button>
      <div className={styles.title}>{resource.title}</div>
      <table className={styles.table}>
        <tbody>
          <tr>
            <td className={styles.label}>{t('metaUrl')}</td>
            <td className={styles.value}>
              <div className={styles.urlRow}>
                <span className={styles.url}>{resource.url}</span>
                <button
                  className={styles.urlOpenBtn}
                  title={t('metaOpenInBrowser')}
                  onClick={() => import("@tauri-apps/plugin-opener").then((mod) => mod.openUrl(resource.url))}
                >
                  ↗
                </button>
              </div>
            </td>
          </tr>
          <tr>
            <td className={styles.label}>{t('metaSavedAt')}</td>
            <td className={styles.value}>
              {new Date(resource.created_at).toLocaleString()}
            </td>
          </tr>
          {folderPath.length > 0 && (
            <tr>
              <td className={styles.label}>{t('metaFolder')}</td>
              <td className={styles.value}>
                <span className={styles.breadcrumb}>
                  {folderPath.map((f, i) => (
                    <span key={f.id}>
                      {i > 0 && <span className={styles.breadcrumbSep}>/</span>}
                      <span
                        className={styles.breadcrumbItem}
                        onClick={() => onNavigateToFolder?.(f.id)}
                      >
                        {f.name}
                      </span>
                    </span>
                  ))}
                </span>
              </td>
            </tr>
          )}
          {tags.length > 0 && (
            <tr>
              <td className={styles.label}>{t('metaTags')}</td>
              <td className={styles.value}>
                <div className={styles.tagRow}>
                  {tags.map((tag) => (
                    <span key={tag.id} className={styles.tagBadge}>
                      <span className={styles.tagDot} style={{ backgroundColor: tag.color }} />
                      {tag.name}
                    </span>
                  ))}
                </div>
              </td>
            </tr>
          )}
        </tbody>
      </table>

      {editing && (
        <ResourceEditDialog
          resource={resource}
          onSave={() => {}}
          onClose={() => setEditing(false)}
        />
      )}
    </div>
  );
}
```

变化点：
- 引入 `useState` `editing` 和 `ResourceEditDialog`
- 在 `.meta` 根 div 内、`.title` 之前加一个 `.editBtn` 按钮
- 文件末尾条件渲染 `ResourceEditDialog`
- `ResourceEditDialog` 的 `onSave` 传空函数，因为后端会 emit `data:resource-changed`，`PreviewPanel` 自己 listen 并更新

### - [ ] Step 3.3: 新增 .editBtn 样式

在 `src/components/ResourceMeta.module.css` 第 1-4 行 `.meta` 规则中添加 `position: relative`，并在文件末尾追加 `.editBtn` 规则：

修改 `.meta`：
```diff
 .meta {
   padding: var(--spacing-md) var(--spacing-lg);
   border-bottom: 1px solid var(--color-border-light);
+  position: relative;
 }
```

在文件末尾（`.tagDot` 规则之后）追加：

```css
.editBtn {
  position: absolute;
  top: var(--spacing-md);
  right: var(--spacing-lg);
  width: 20px;
  height: 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--color-border);
  border-radius: 4px;
  background: transparent;
  color: var(--color-text-muted);
  cursor: pointer;
  font-size: 12px;
  padding: 0;
  line-height: 1;
}

.editBtn:hover {
  color: var(--color-accent);
  border-color: var(--color-accent);
}
```

同时确保 `.title` 右侧有足够空间避免被按钮压住：

```diff
 .title {
   font-size: var(--font-size-lg);
   font-weight: 600;
   color: var(--color-text-primary);
   margin-bottom: var(--spacing-sm);
   word-break: break-word;
+  padding-right: 28px;
 }
```

### - [ ] Step 3.4: 类型检查

Run: `npx tsc --noEmit`
Expected: 无错误。若报 `ResourceEditDialog` 的 import 路径问题，确认路径为 `@/components/Sidebar/ResourceEditDialog`（alias 别名）。

### - [ ] Step 3.5: Commit

```bash
git add src/components/ResourceMeta.tsx src/components/ResourceMeta.module.css src/locales/zh/sidebar.json src/locales/en/sidebar.json
git commit -m "feat(preview): add edit button in metadata header"
```

---

## Task 4: PreviewPanel 标注可点击跳转到阅读器

**对应 spec 第 4 节。**

**Files:**
- Modify: `src/components/PreviewPanel.tsx`
- Modify: `src/components/PreviewPanel.module.css`
- Modify: `src/components/Layout.tsx:382-386`

### - [ ] Step 4.1: PreviewPanel 新增 prop 并让 highlight item 可点击

修改 `src/components/PreviewPanel.tsx`。接口和主体 change 如下：

接口处 diff（第 12-15 行）：

```diff
 interface PreviewPanelProps {
   resource: Resource;
   onNavigateToFolder?: (folderId: string) => void;
+  onOpenHighlight?: (resourceId: string, highlightId: string) => void;
 }
```

函数签名与参数解构（第 17 行）：

```diff
-export function PreviewPanel({ resource: initialResource, onNavigateToFolder }: PreviewPanelProps) {
+export function PreviewPanel({ resource: initialResource, onNavigateToFolder, onOpenHighlight }: PreviewPanelProps) {
```

高亮 item 的 JSX（第 64-76 行）替换为：

```tsx
{highlights.map((hl) => {
  const comments = getCommentsForHighlight(hl.id);
  return (
    <div
      key={hl.id}
      className={`${styles.highlightItem} ${onOpenHighlight ? styles.highlightClickable : ""}`}
      style={{ borderLeftColor: hl.color }}
      onClick={() => onOpenHighlight?.(resource.id, hl.id)}
      role={onOpenHighlight ? "button" : undefined}
      tabIndex={onOpenHighlight ? 0 : undefined}
    >
      <div className={styles.highlightText}>{hl.text_content}</div>
      {comments.map((c) => (
        <div
          key={c.id}
          className={styles.commentItem}
          onClickCapture={(e) => {
            // Let markdown-rendered links work; stop bubbling only if user clicked a link
            if ((e.target as HTMLElement).closest("a")) {
              e.stopPropagation();
            }
          }}
        >
          <MarkdownContent content={c.content} />
        </div>
      ))}
    </div>
  );
})}
```

注意：`onClickCapture` 中只在点击链接（`<a>`）时 stopPropagation，点击评论文本其他位置仍会触发外层跳转。

### - [ ] Step 4.2: 增加 hover 样式

在 `src/components/PreviewPanel.module.css` 末尾（`.noteItem` 之后）追加：

```css
.highlightClickable {
  cursor: pointer;
  transition: background 0.15s;
}

.highlightClickable:hover {
  background: var(--color-bg-hover);
}
```

### - [ ] Step 4.3: Layout.tsx 透传 onOpenHighlight

修改 `src/components/Layout.tsx` 第 382-386 行：

```diff
 <PreviewPanel
   key={selectedResource.id}
   resource={selectedResource}
   onNavigateToFolder={(folderId) => setSelectedFolderId(folderId)}
+  onOpenHighlight={(_rid, hid) => onOpenResource(selectedResource, hid)}
 />
```

`_rid` 下划线前缀表示"intentionally unused"；`selectedResource` 由闭包捕获（就是当前 PreviewPanel 展示的那个资料）。

### - [ ] Step 4.4: 类型检查

Run: `npx tsc --noEmit`
Expected: 无错误。

### - [ ] Step 4.5: Commit

```bash
git add src/components/PreviewPanel.tsx src/components/PreviewPanel.module.css src/components/Layout.tsx
git commit -m "feat(preview): click highlight in preview to jump to position in reader"
```

---

## Task 5: 去掉 PDF+ 按钮，改为右键菜单导入

**对应 spec 第 5 节。**

**Files:**
- Create: `src/lib/importPdf.ts`
- Modify: `src/components/Sidebar/ResourceList.tsx`
- Modify: `src/components/Sidebar/FolderTree.tsx`

### - [ ] Step 5.1: 新建共享导入函数 `src/lib/importPdf.ts`

```ts
import { open } from "@tauri-apps/plugin-dialog";
import toast from "react-hot-toast";
import i18n from "@/i18n";
import * as cmd from "@/lib/commands";

/** Open file picker (filtered to .pdf) and import selected file into target folder. */
export async function importPdfToFolder(folderId: string): Promise<void> {
  try {
    const selected = await open({
      multiple: false,
      filters: [{ name: "PDF", extensions: ["pdf"] }],
    });
    if (!selected) return;

    const filePath = typeof selected === "string" ? selected : (selected as { path: string }).path;
    await cmd.importPdf(filePath, folderId);
    toast.success(i18n.t("saveSuccess", { ns: "common" }));
  } catch (err) {
    toast.error(String(err));
  }
}
```

备注：用 `i18n.t(...)` 而非 hook，方便在非组件环境下（但这里仍在事件回调里）使用，也保持与现有代码中 `i18n.t` 的惯用法一致。

### - [ ] Step 5.2: ResourceList.tsx 删除 PDF+ 按钮并加空白处右键菜单

修改 `src/components/Sidebar/ResourceList.tsx`：

**(a) 删除现有 `handleImportPdf` 函数（第 224-239 行）**

**(b) 删除顶部 `PDF+` 按钮（第 313-319 行）**：

```diff
         <div className={styles.sortControls}>
-          <button
-            className={styles.importBtn}
-            onClick={handleImportPdf}
-            title={tReader("importPdf")}
-          >
-            PDF+
-          </button>
           <select
             className={styles.sortSelect}
```

**(c) 引入 `importPdfToFolder`**：在文件顶部 import 区加：

```ts
import { importPdfToFolder } from "@/lib/importPdf";
import { ContextMenu, type MenuItem } from "@/components/ContextMenu";
import { ALL_RESOURCES_ID } from "@/types";
```

（`ContextMenu` 若已 import 则跳过；`ALL_RESOURCES_ID` 确保可引用）

**(d) 新增 state + handler**：在组件函数体内（与其他 useState 并列）加：

```ts
const [emptyMenu, setEmptyMenu] = useState<{ x: number; y: number } | null>(null);
```

在 `handleKeyDown` 前加：

```ts
const isRealFolder = folderId !== null && folderId !== ALL_RESOURCES_ID;

function handleListContextMenu(e: React.MouseEvent) {
  if (!isRealFolder) return;
  // Resource items' onContextMenu calls stopPropagation (line 177 of this file),
  // so this handler only fires when right-clicking on empty area or emptyState div.
  e.preventDefault();
  setEmptyMenu({ x: e.clientX, y: e.clientY });
}
```

**(e) 给 `.listScroll` 容器加 onContextMenu**（第 337 行）：

```diff
-      <div className={styles.listScroll}>
+      <div className={styles.listScroll} onContextMenu={handleListContextMenu}>
```

说明：资源项内部的 `handleContextMenu` 已调用 `e.stopPropagation()`（`ResourceList.tsx:177`），所以挂在外层的 handler 只会在空白区域或 `.emptyState` 占位 div 上触发，无需额外 target 判断。

**(f) 渲染右键菜单**（放在现有 `ResourceContextMenu` JSX 附近，第 382 行之前添加）：

```tsx
{emptyMenu && isRealFolder && (
  <ContextMenu
    x={emptyMenu.x}
    y={emptyMenu.y}
    items={[
      {
        label: tReader("importPdf"),
        onClick: () => importPdfToFolder(folderId!),
      },
    ]}
    onClose={() => setEmptyMenu(null)}
  />
)}
```

### - [ ] Step 5.3: FolderTree.tsx 右键菜单加"导入 PDF"

修改 `src/components/Sidebar/FolderTree.tsx`：

**(a) Import**：

顶部 import 区新增：

```ts
import { importPdfToFolder } from "@/lib/importPdf";
```

**(b) Menu items**：第 171-190 行 `menuItems` 改为：

```tsx
const menuItems: MenuItem[] = contextMenu
  ? [
      {
        label: t('newSubfolder'),
        onClick: () => {
          setSubfolderTarget(contextMenu.folderId);
          setSubfolderName("");
        },
      },
      {
        label: t('importPdf', { ns: 'reader' }),
        onClick: () => importPdfToFolder(contextMenu.folderId),
      },
      {
        label: t('edit', { ns: 'common' }),
        onClick: () => setEditFolder({ id: contextMenu.folderId, name: contextMenu.folderName }),
      },
      {
        label: t('delete', { ns: 'common' }),
        danger: true,
        onClick: () => setDeleteFolder({ id: contextMenu.folderId, name: contextMenu.folderName }),
      },
    ]
  : [];
```

备注：这里的 `contextMenu.folderId` 只会在真实 folder（非 `__all__`）上触发，因为 FolderTree 的右键只挂在 `DraggableFolderItem`（folder row）上，"全部资料" 按钮不触发右键菜单（`.allResources` button 没有 `onContextMenu`，见第 200-205 行）。

### - [ ] Step 5.4: 删除 ResourceList 中 unused reader i18n import（若 tReader 仅被 PDF+ 按钮使用）

检查 `ResourceList.tsx` 中 `tReader` 变量：若除 `tReader("importPdf")` 外还用于 `emptyMenu` 的菜单项（见 Step 5.2-f），则保留；否则可能需要清理无效 import。预期 `tReader` 仍被使用，无需删除。

### - [ ] Step 5.5: 类型 + 构建检查

Run: `npx tsc --noEmit`
Expected: 无错误。

如果报 `ContextMenu`/`ALL_RESOURCES_ID` 未使用，确认在 Step 5.2 中确实引用了它们。

### - [ ] Step 5.6: Commit

```bash
git add src/lib/importPdf.ts src/components/Sidebar/ResourceList.tsx src/components/Sidebar/FolderTree.tsx
git commit -m "refactor(resources): replace PDF+ button with folder/list context menu upload"
```

---

## Task 6: 外观 / 同步设置页加 subheading 和 divider

**对应 spec 第 6 节。**

**Files:**
- Modify: `src/locales/zh/settings.json`
- Modify: `src/locales/en/settings.json`
- Modify: `src/locales/zh/sync.json`
- Modify: `src/locales/en/sync.json`
- Modify: `src/components/Settings/AppearancePage.tsx`
- Modify: `src/components/Settings/SyncPage.tsx`

### - [ ] Step 6.1: i18n 新增 `settings.theme`

`src/locales/zh/settings.json`：

```diff
 {
   "title": "外观",
+  "theme": "主题",
   "themeLight": "浅色",
```

`src/locales/en/settings.json`（需读取文件找到对应位置；预期 `title` 后）：

```diff
 {
   "title": "Appearance",
+  "theme": "Theme",
   "themeLight": "Light",
```

### - [ ] Step 6.2: i18n 新增 `sync.connection`

`src/locales/zh/sync.json`：

```diff
 {
   "title": "同步设置",
+  "connection": "连接",
   "endpointLabel": "Endpoint（可选，留空使用 AWS 默认）",
```

`src/locales/en/sync.json`：

```diff
 {
   "title": "Sync Settings",
+  "connection": "Connection",
   "endpointLabel": "Endpoint (optional, leave empty for AWS default)",
```

（若 en/sync.json 顶部字段顺序不同，找到 `"title"` 行在其后插入即可）

### - [ ] Step 6.3: AppearancePage.tsx 加 subheading + passwordSection

完整替换 `src/components/Settings/AppearancePage.tsx`：

```tsx
import { useTranslation } from "react-i18next";
import type { ThemeMode } from "@/hooks/useTheme";
import settingsStyles from "./Settings.module.css";
import styles from "./AppearancePage.module.css";

interface AppearancePageProps {
  themeMode: ThemeMode;
  onThemeModeChange: (mode: ThemeMode) => void;
}

const THEME_OPTIONS = [
  { id: "light" as ThemeMode, icon: "☀️", labelKey: "themeLight" as const },
  { id: "dark" as ThemeMode, icon: "🌙", labelKey: "themeDark" as const },
  { id: "system" as ThemeMode, icon: "💻", labelKey: "themeSystem" as const },
];

const LANGUAGE_OPTIONS = [
  { value: "zh", label: "中文" },
  { value: "en", label: "English" },
];

export function AppearancePage({ themeMode, onThemeModeChange }: AppearancePageProps) {
  const { t, i18n } = useTranslation("settings");

  return (
    <>
      <h2 className={settingsStyles.heading}>{t("title")}</h2>

      <div className={settingsStyles.form}>
        <h3 className={settingsStyles.subheading}>{t("theme")}</h3>
        <div className={styles.themeOptions}>
          {THEME_OPTIONS.map((opt) => (
            <button
              key={opt.id}
              className={`${styles.themeBtn} ${themeMode === opt.id ? styles.themeBtnActive : ""}`}
              onClick={() => onThemeModeChange(opt.id)}
            >
              <span className={styles.themeIcon}>{opt.icon}</span>
              {t(opt.labelKey)}
            </button>
          ))}
        </div>
      </div>

      <div className={settingsStyles.passwordSection}>
        <h3 className={settingsStyles.subheading}>{t("language")}</h3>
        <div className={styles.themeOptions}>
          {LANGUAGE_OPTIONS.map((opt) => (
            <button
              key={opt.value}
              className={`${styles.themeBtn} ${i18n.language === opt.value ? styles.themeBtnActive : ""}`}
              onClick={() => i18n.changeLanguage(opt.value)}
            >
              {opt.label}
            </button>
          ))}
        </div>
      </div>
    </>
  );
}
```

### - [ ] Step 6.4: SyncPage.tsx 加 subheading + passwordSection

在 `src/components/Settings/SyncPage.tsx` 第 147 行（`<div className={styles.form}>` 开头）之后、第一个 `<label>` 之前插入 subheading；并把 maintenance 区块用 `passwordSection` 包裹。

具体 diff：

第 147-149 行：

```diff
       <div className={styles.form}>
+        <h3 className={styles.subheading}>{t('connection')}</h3>
         <label className={styles.label}>
           <span>{t('endpointLabel')}</span>
```

第 248-315 行（`{hasCredentials && (...)}` 整个块）：将当前的 fragment `<>...</>` 替换为 `<div className={styles.passwordSection}>...</div>`：

```diff
       {hasCredentials && (
-        <>
+        <div className={styles.passwordSection}>
           <h3 className={styles.subheading}>{t('maintenance')}</h3>
           <div className={styles.actions}>
             <button
               className={styles.secondary}
               onClick={async () => {
                 setCompacting(true);
                 try {
                   const result = await cmd.forceCompact();
                   toast.success(result);
                 } catch (err) {
                   toast.error(t('compactFailed', { error: formatError(err) }));
                 } finally {
                   setCompacting(false);
                 }
               }}
               disabled={compacting}
             >
               {compacting ? t('compacting') : t('forceCompact')}
             </button>
             <button
               className={styles.secondary}
               onClick={handleScanOrphans}
               disabled={scanning}
             >
               {scanning ? t('scanning') : t('cleanOrphans')}
             </button>
           </div>

           {/* Scan result panel */}
           {scanResult && (
             <div className={styles.info} style={{ marginTop: "var(--spacing-md)" }}>
               {scanResult.count === 0 ? (
                 <span>{t('noOrphansFound')}</span>
               ) : (
                 <>
                   <div dangerouslySetInnerHTML={{ __html: t('orphansFound', { count: scanResult.count, size: formatSize(scanResult.total_size) }) }} />
                   <div className={styles.orphanList}>
                     {scanResult.items.map((item) => (
                       <div key={item.resource_id}>
                         {item.resource_id}  {formatSize(item.size)}
                       </div>
                     ))}
                   </div>
                   <div className={styles.modalActions}>
                     <button
                       className={styles.secondary}
                       onClick={() => setScanResult(null)}
                     >
                       {t('cancel')}
                     </button>
                     <button
                       className={styles.danger}
                       onClick={() => {
                         setConfirmInput("");
                         setShowConfirmModal(true);
                       }}
                     >
                       {t('startCleanup')}
                     </button>
                   </div>
                 </>
               )}
             </div>
           )}
-        </>
+        </div>
       )}
```

### - [ ] Step 6.5: 类型检查

Run: `npx tsc --noEmit`
Expected: 无错误。

### - [ ] Step 6.6: Commit

```bash
git add src/locales/zh/settings.json src/locales/en/settings.json src/locales/zh/sync.json src/locales/en/sync.json src/components/Settings/AppearancePage.tsx src/components/Settings/SyncPage.tsx
git commit -m "style(settings): add subheadings and dividers to Appearance/Sync pages"
```

---

## 最终手动验收

（所有 commit 完成后，由用户手动验证。AI 无法验证 GUI 表现。）

启动应用：`VITE_DEBUG=1 npm run tauri dev`

逐项验收：

1. **Task 1**：悬停左下角齿轮按钮，tooltip 显示"设置"（中文）或"Settings"（英文）
2. **Task 2**：在第三栏预览面板看不同长度 URL 的资料，打开按钮紧跟 URL 末尾且高度与 URL 字符行高一致；URL 很长换行时按钮跟在最后一行末尾
3. **Task 3**：预览面板右上角有铅笔按钮，悬停显示"编辑"；点击弹出编辑弹窗；保存后预览面板刷新
4. **Task 4**：预览面板中点击一个高亮条目 → 打开该资源的阅读器 Tab，滚动到该高亮位置；点击评论区内的 Markdown 链接 → 系统浏览器打开该链接、不会误触发跳转
5. **Task 5**：第二栏顶部无 "PDF+" 按钮；在 Sidebar 真实文件夹上右键 → 菜单含"导入 PDF"，点击选文件后成功导入到该文件夹；在"全部资料" 按钮上右键 → 无菜单；选中真实文件夹后在第二栏空白处右键 → 弹出含"导入 PDF"的菜单；选中"全部资料" 时第二栏空白处右键 → 无菜单
6. **Task 6**：外观页第一项"主题"前有子标题，主题和语言之间有水平分隔线；同步页 S3 配置区上方有"连接"子标题，维护区与配置区之间有水平分隔线
